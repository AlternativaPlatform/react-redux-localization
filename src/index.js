import localize from './localize';
import localizeKey from './localize-key';
import { setLocale, getLocale, localeReducer } from './locale-reducer';

export { localize, localizeKey, setLocale, getLocale, localeReducer };
